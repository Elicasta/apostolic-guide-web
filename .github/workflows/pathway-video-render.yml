name: Pathway video render

on:
  repository_dispatch:
    types: [pathway-video-render]

permissions:
  contents: read

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      VIDEO_STUDIO_SUPABASE_URL: ${{ secrets.VIDEO_STUDIO_SUPABASE_URL }}
      VIDEO_STUDIO_SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.VIDEO_STUDIO_SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Check out renderer
        uses: actions/checkout@v4

      - name: Install ffmpeg and fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg fonts-dejavu-core

      - name: Write render payload
        env:
          RENDER_PAYLOAD: ${{ toJson(github.event.client_payload) }}
        run: |
          printf '%s' "$RENDER_PAYLOAD" > /tmp/pathway-video-payload.json

      - name: Render and upload video
        run: |
          python3 scripts/render_pathway_video.py /tmp/pathway-video-payload.json /tmp/pathway-video.mp4
