import { notFound } from "next/navigation";import { resolveGuestInvite } from "@/studio/guest-repository";import GuestGreenRoom from "./guest-green-room";import "./guest.css";export const dynamic="force-dynamic";export default async function GuestPage({params}:{params:Promise<{token:string}>}){const {token}=await params;const invite=await resolveGuestInvite(token).catch(()=>null);if(!invite)notFound();const guest=invite.studio_episode_guests as {id:string;display_name:string};const episode=invite.studio_episodes as {title:string};return <GuestGreenRoom token={token} guestId={guest.id} guestName={guest.display_name} episodeTitle={episode.title}/>;}
