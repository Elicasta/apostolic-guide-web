"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import type { StudioProgramState } from "@/studio/types";
import { subscribeStudioRoom,type LiveKitConnection } from "@/studio/livekit-client";
import { connectOutputHostMedia, type OutputHostBridgeStatus } from "./host-media-receiver";

type OutputAsset={id:string;asset_type:string;label?:string|null;snapshot_data?:Record<string,unknown>|null;custom_data?:Record<string,unknown>|null};
type OutputSnapshot={state:StudioProgramState|null;stateVersion:number;assets:OutputAsset[];session:{studio_episodes?:{title?:string}|null}};
type GuestFeed={identity:string;name:string;stream:MediaStream};type RemoteMedia={url:string;token:string;room:string;identity:string};

export default function StudioOutputClient({sessionId,token}:{sessionId:string;token:string}){
 const[snapshot,setSnapshot]=useState<OutputSnapshot|null>(null),[failed,setFailed]=useState(false),[hostStream,setHostStream]=useState<MediaStream|null>(null),[hostBridgeStatus,setHostBridgeStatus]=useState<OutputHostBridgeStatus>("waiting"),[guestFeeds,setGuestFeeds]=useState<GuestFeed[]>([]),[guestMediaState,setGuestMediaState]=useState("idle");const peerRef=useRef<RTCPeerConnection|null>(null),liveKitRef=useRef<LiveKitConnection|null>(null),audioNodes=useRef(new Map<string,HTMLMediaElement>());
 useEffect(()=>{let alive=true;let timer:ReturnType<typeof setTimeout>|undefined;async function refresh(){try{const response=await fetch(`/api/studio/output/${sessionId}?token=${encodeURIComponent(token)}`,{cache:"no-store"});if(!response.ok)throw new Error("output unavailable");const next=await response.json() as OutputSnapshot;if(alive){setSnapshot(current=>!current||next.stateVersion>=current.stateVersion?next:current);setFailed(false);}}catch{if(alive)setFailed(true);}finally{if(alive)timer=setTimeout(refresh,750);}}refresh();return()=>{alive=false;if(timer)clearTimeout(timer);};},[sessionId,token]);
 useEffect(()=>{if(!token)return;let cancelled=false;void connectOutputHostMedia(sessionId,token,stream=>{if(!cancelled)setHostStream(stream);},status=>{if(!cancelled)setHostBridgeStatus(status);}).then(peer=>{if(cancelled)peer.close();else peerRef.current=peer;}).catch(()=>{if(!cancelled)setHostBridgeStatus("failed");});return()=>{cancelled=true;peerRef.current?.close();peerRef.current=null;};},[sessionId,token]);
 useEffect(()=>{if(!token)return;let cancelled=false;async function connect(){setGuestMediaState("connecting");try{const r=await fetch(`/api/studio/output/${sessionId}/remote-media?token=${encodeURIComponent(token)}`,{cache:"no-store"});if(!r.ok)throw new Error("Remote media unavailable");const p=await r.json() as {remoteMedia:RemoteMedia|null};if(!p.remoteMedia){setGuestMediaState("disabled");return;}const connection=await subscribeStudioRoom(p.remoteMedia,{onVideo:(identity,name,element)=>{if(cancelled||!identity.startsWith("guest-"))return;const media=element.srcObject;if(!(media instanceof MediaStream))return;setGuestFeeds(current=>{const next=current.filter(g=>g.identity!==identity);return[...next,{identity,name,stream:media}];});},onAudio:(identity,element)=>{if(cancelled||!identity.startsWith("guest-")||audioNodes.current.has(identity))return;element.autoplay=true;element.style.display="none";document.body.appendChild(element);audioNodes.current.set(identity,element);void element.play().catch(()=>{});},onRemove:identity=>{setGuestFeeds(current=>current.filter(g=>g.identity!==identity));const audio=audioNodes.current.get(identity);audio?.remove();audioNodes.current.delete(identity);}});if(cancelled)connection.disconnect();else{liveKitRef.current=connection;setGuestMediaState("connected");}}catch{if(!cancelled)setGuestMediaState("failed");}}void connect();return()=>{cancelled=true;liveKitRef.current?.disconnect();liveKitRef.current=null;audioNodes.current.forEach(node=>node.remove());audioNodes.current.clear();setGuestFeeds([]);};},[sessionId,token]);
 const assets=useMemo(()=>new Map((snapshot?.assets??[]).map(asset=>[asset.id,asset])),[snapshot?.assets]);const state=snapshot?.state,scripture=state?.activeScriptureId?assets.get(state.activeScriptureId):undefined,question=state?.activeQuestionId?assets.get(state.activeQuestionId):undefined;
 if(!token)return <main className="studio-output studio-output-error">Output token required</main>;if(!snapshot&&failed)return <main className="studio-output studio-output-error">Output unavailable</main>;if(!state)return <main className="studio-output studio-output-holding"><AGMark/><p>Preparing broadcast</p></main>;
 return <main className={`studio-output scene-${state.currentSceneId}`}><div className="studio-output-bg"/><div className="studio-clip-safe-guide"/><Scene sceneId={state.currentSceneId} episodeTitle={snapshot?.session?.studio_episodes?.title} scripture={scripture} question={question} hostStream={hostStream} guests={guestFeeds}/><div className="studio-output-overlays">{[...state.activeOverlays].sort((a,b)=>a.layer-b.layer).map(overlay=><Overlay key={overlay.id} asset={overlay.assetId?assets.get(overlay.assetId):undefined} type={overlay.type}/>)}</div>{failed?<span className="studio-output-reconnecting">Reconnecting state</span>:null}{hostBridgeStatus==="failed"?<span className="studio-output-source-warning">Host media disconnected</span>:null}{guestMediaState==="failed"?<span className="studio-output-guest-warning">Guest media disconnected</span>:null}</main>;
}

function Scene({sceneId,episodeTitle,scripture,question,hostStream,guests}:{sceneId:string;episodeTitle?:string;scripture?:OutputAsset;question?:OutputAsset;hostStream:MediaStream|null;guests:GuestFeed[]}){const g=(i:number)=>guests[i-1];if(sceneId==="black")return <div className="studio-black"/>;if(sceneId==="holding")return <div className="studio-center-card"><AGMark/><span>APOSTOLIC GUIDE</span><h1>We’ll be right back.</h1><p>Please stand by.</p></div>;if(sceneId==="scripture-full")return <ScriptureCard asset={scripture} full/>;if(sceneId==="question-full")return <QuestionCard asset={question} full/>;if(sceneId==="pathway-cta")return <div className="studio-center-card"><span>KEEP STUDYING</span><h1>{episodeTitle??"Apostolic Guide"}</h1><p>Continue through the complete Scripture pathway on Apostolic Guide.</p></div>;if(sceneId==="host-scripture")return <div className="studio-split studio-safe-composition"><HostVideo stream={hostStream}/><ScriptureCard asset={scripture}/></div>;if(sceneId==="panel-scripture")return <div className="studio-panel-scripture studio-safe-composition"><div className="studio-panel-mini"><HostVideo stream={hostStream}/><GuestTile feed={g(1)} index={1}/></div><ScriptureCard asset={scripture}/></div>;if(sceneId==="split")return <div className="studio-interview-grid studio-safe-composition"><HostVideo stream={hostStream}/><GuestTile feed={g(1)} index={1}/></div>;if(sceneId==="panel-grid")return <div className="studio-panel-grid studio-safe-composition"><HostVideo stream={hostStream}/><GuestTile feed={g(1)} index={1}/><GuestTile feed={g(2)} index={2}/><GuestTile feed={g(3)} index={3}/></div>;if(sceneId==="panel-question")return <div className="studio-panel-question studio-safe-composition"><div className="studio-panel-mini"><HostVideo stream={hostStream}/><GuestTile feed={g(1)} index={1}/></div><QuestionCard asset={question}/></div>;if(sceneId==="title-full")return <div className="studio-center-card"><span>APOSTOLIC GUIDE</span><h1>{episodeTitle??"Live"}</h1></div>;return <div className="studio-host-safe studio-safe-composition"><HostVideo stream={hostStream} full/></div>;}
function HostVideo({stream,full=false}:{stream:MediaStream|null;full?:boolean}){const videoRef=useRef<HTMLVideoElement>(null);useEffect(()=>{if(videoRef.current&&stream)videoRef.current.srcObject=stream;},[stream]);if(!stream)return <div className={`studio-camera-placeholder ${full?"studio-camera-full":""}`}><AGMark/><span>HOST CAMERA CONNECTING</span></div>;return <div className={`studio-host-video ${full?"studio-camera-full":""}`}><video ref={videoRef} autoPlay playsInline/></div>;}
function GuestTile({feed,index}:{feed?:GuestFeed;index:number}){const ref=useRef<HTMLVideoElement>(null);useEffect(()=>{if(ref.current&&feed?.stream)ref.current.srcObject=feed.stream;},[feed?.stream]);if(!feed)return <div className="studio-guest-tile"><div className="studio-ag-mark">AG</div><span>GUEST {index}</span><small>Waiting in remote studio</small></div>;return <div className="studio-guest-video"><video ref={ref} autoPlay muted playsInline/><div className="studio-guest-name"><span>{feed.name}</span><small>AG LIVE</small></div></div>;}
function ScriptureCard({asset,full=false}:{asset?:OutputAsset;full?:boolean}){const data=asset?.snapshot_data??{};return <div className={`studio-scripture ${full?"full":""}`}><span>{String(data.translation??"SCRIPTURE")}</span><h2>{String(data.reference??asset?.label??"Scripture")}</h2><p>{String(data.text??data.explanation??"Scripture text unavailable")}</p></div>;}
function QuestionCard({asset,full=false}:{asset?:OutputAsset;full?:boolean}){const data=asset?.custom_data??asset?.snapshot_data??{};return <div className={`studio-question ${full?"full":""}`}><span>QUESTION</span><h2>{String(data.question??asset?.label??"Live question")}</h2></div>;}
function Overlay({asset,type}:{asset?:OutputAsset;type:string}){const data=asset?.custom_data??asset?.snapshot_data??{};if(type==="lower_third")return <div className="studio-lower-third"><strong>{String(data.primaryText??asset?.label??"Apostolic Guide")}</strong><span>{String(data.secondaryText??"")}</span></div>;if(type==="question")return <div className="studio-question-overlay">{String(data.question??asset?.label??"Question")}</div>;if(type==="cta")return <div className="studio-cta-overlay">{String(data.text??asset?.label??"Continue on Apostolic Guide")}</div>;return null;}
function AGMark(){return <div className="studio-ag-mark">AG</div>;}
